# Work log

This is just to log the work that is going on in this. 

Current objectives

- Get the imu value and timestamp them at the time of capture
	- BMI270 sensor
	- Make sure you can get at least around 50 to 100 Hz frequency without any drop
- Get the images and timestamp them at the time of capture. 
- Get both of them together. 
- Be able to capture them on the pc side, without much of frame drop. 
- Utilize ros on the PC size and try to make a ros bag out of the data. 
- Utilize some of the ros algorithms to build a slam around it. 
- See methods to calibrate IMU 
- See methods to calibrate the camera sensor. 
- Put everything together and see if a good slam could be built. 
- Break down the slam algorithm. 
- Try to build the slam algorithm on the esp32S3 itself. 
- See if you can do this on the better camera with better lens. 